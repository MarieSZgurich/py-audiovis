import soundfile as sf # reads audio data
import numpy as np # handles audio data
from pydub import AudioSegment
from pydub.playback import play # plays audio file
from multiprocessing import Process # let audio and visual work simultaneously

